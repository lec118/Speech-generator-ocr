{
  "name": "speech-generator-ocr",
  "version": "0.1.0",
  "private": true,
  "workspaces": [
    "apps/*",
    "packages/*"
  ],
  "scripts": {
    "dev": "turbo run dev",
    "build": "turbo run build",
    "test": "turbo run test",
    "lint": "turbo run lint",
    "type-check": "turbo run type-check",
    "clean": "turbo run clean && rimraf node_modules"
  },
  "devDependencies": {
    "autoprefixer": "^10",
    "next": "14.2.5",
    "postcss": "^8",
    "rimraf": "^5.0.5",
    "tailwindcss": "^3",
    "turbo": "^2.0.0",
    "typescript": "^5.3.3"
  },
  "packageManager": "pnpm@8.15.4",
  "engines": {
    "node": "20.x",
    "pnpm": ">=8.15.4"
  }
}
