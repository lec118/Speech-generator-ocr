{
  "installCommand": "corepack enable && corepack prepare pnpm@8.15.4 --activate && pnpm install",
  "buildCommand": "pnpm turbo run build --filter=@apps/web",
  "outputDirectory": "apps/web/.next"
}
